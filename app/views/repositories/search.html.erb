<h1>Search Github Repositories</h1>
<%= form_tag '/search' do %>
  <%= label_tag :query %>
  <%= text_field_tag :query %>
  <%= submit_tag "Search Github!" %>
<% end %>

<div>
  <% if @repos %>
  <ul> <% @repos.each do |repo| %>
      <li> <%= repo['name'] %> </li>
      <li> <%= repo['description'] %> </li>
      <li> <%= link_to repo['html_url'], repo['html_url']  %> </li>
      <br>
      <% end %>
  <% else %>
    <li>Tetris</li>
    <li>A C implementation of Tetris using Pennsim through LC4</li>
    <li> <a href="https://github.com/dtrupenn/Tetris">https://github.com/dtrupenn/Tetris</a> </li>
  <% end %>
  </ul>
</div>
